# WSI Viewer 기본 의존성
PySide6>=6.4.0
openslide-python>=1.2.0
cachetools>=5.0.0
Pillow>=9.0.0

# 이미지 처리 (조직 감지용)
opencv-python>=4.7.0
numpy>=1.21.0

# HTTP 클라이언트 (서버 API 통신용)
requests>=2.25.0

# 로깅 및 유틸리티
pathlib  # Python 3.4+ 기본 포함
requests
#pip install openslide-bin